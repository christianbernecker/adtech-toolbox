{
  "name": "adtech-toolbox",
  "version": 2,
  "buildCommand": "cd json-explorer && npm install && npm run build",
  "outputDirectory": "json-explorer/dist",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "github": {
    "silent": false
  }
} 